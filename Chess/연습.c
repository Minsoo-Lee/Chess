#include <stdio.h>

int main(void) {
	int x = 0;
	int y = 3;
	int z = 3;
	printf("%d %d %d", x, y, z);
	while (x != y) {
		if (x == z)
			printf("∏ÿ√„ %d", x);
		else
			printf("∞Ëº” %d", x);
		x++;
	}
	return 0;
}